<h1>Dual forms</h1>

<%= form_tag('/', method: :post, id: 'form-one') do %>
  <%= submit_tag 'Submit one' %>
<% end %>

<%= form_tag('/', method: :post, id: 'form-two') do %>
  <%= submit_tag 'Submit two' %>
<% end %>

<%= button_to 'Action button', '/' %>
